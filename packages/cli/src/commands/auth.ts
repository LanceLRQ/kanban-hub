import type { Command } from "commander";
import type { CliContext } from "../context";

/** 占位实现：任务 5 在这里加上 login / logout / whoami */
export function registerAuth(_program: Command, _ctx: CliContext): void {}
