import type { Command } from "commander";
import type { CliContext } from "../context";

/** 占位实现：任务 6 在这里加上 register 命令 */
export function registerRegister(_program: Command, _ctx: CliContext): void {}
